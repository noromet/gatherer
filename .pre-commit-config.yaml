repos:
  - repo: https://github.com/psf/black
    rev: 25.1.0  # Use the latest stable version
    hooks:
      - id: black

  - repo: https://github.com/PyCQA/pylint
    rev: v3.3.6  # Use the latest stable version
    hooks:
      - id: pylint
        args:
          - --disable=import-error